# 对话功能优化说明

## 完成时间
2026-05-09

## 优化内容

### 1. 输入窗口收缩功能

**功能描述**:
- 添加了输入框收缩/展开按钮
- 用户可以点击按钮收缩输入框，节省屏幕空间
- 收缩后仍然可以看到控制栏和联网开关
- 平滑的动画过渡效果

**使用方法**:
1. 在聊天输入框顶部找到"收缩"按钮
2. 点击按钮收缩输入框
3. 再次点击"展开"按钮恢复输入框

**技术实现**:
- 使用 CSS `transition` 实现平滑动画
- 使用 `max-h-0` 和 `opacity-0` 实现收缩效果
- 保持控制栏始终可见

### 2. 联网搜索功能

**功能描述**:
- 添加了联网搜索开关
- 用户可以选择是否启用联网搜索
- 启用后会显示"已启用"标识
- 状态会保存在组件状态中

**使用方法**:
1. 在输入框顶部找到"联网搜索"复选框
2. 勾选复选框启用联网搜索
3. 启用后右侧会显示"已启用"标识
4. 取消勾选即可关闭联网搜索

**技术实现**:
- 使用 `useState` 管理联网搜索状态
- 使用复选框控件实现开关
- 添加视觉反馈（图标 + 文字）

## 界面布局

```
┌─────────────────────────────────────────────────┐
│ [收缩▼] [☑ 联网搜索]              [已启用]      │
├─────────────────────────────────────────────────┤
│                                                 │
│  输入框区域（可收缩）                            │
│  - 文件列表                                      │
│  - 文本输入框                                    │
│  - 操作按钮（语音、发送等）                       │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 修改的文件

**文件**: `web/app/components/base/chat/chat/chat-input-area/index.tsx`

**主要修改**:
1. 添加状态管理:
   ```typescript
   const [isCollapsed, setIsCollapsed] = useState(false)
   const [enableWebSearch, setEnableWebSearch] = useState(false)
   ```

2. 添加控制栏:
   - 收缩/展开按钮
   - 联网搜索开关
   - 状态指示器

3. 修改输入区域样式:
   - 添加过渡动画
   - 根据收缩状态调整高度和透明度

## 视觉效果

### 收缩按钮
- 图标: 向下箭头（收缩时旋转180度）
- 文字: "收缩" / "展开"
- 悬停效果: 背景色变化

### 联网搜索开关
- 图标: 地球图标
- 复选框: 标准复选框样式
- 启用标识: 蓝色背景 + 信息图标

### 动画效果
- 收缩/展开: 300ms 平滑过渡
- 图标旋转: 300ms 平滑旋转
- 悬停效果: 颜色渐变

## 后续优化建议

### 1. 联网搜索功能集成
目前联网搜索只是一个开关，需要集成到实际的发送逻辑中：
- 在 `handleSend` 或 `doActualSend` 中传递 `enableWebSearch` 状态
- 后端需要根据这个标志决定是否进行联网搜索
- 可以考虑添加搜索引擎选择（Google、Bing等）

### 2. 状态持久化
- 将收缩状态保存到 localStorage
- 将联网搜索偏好保存到用户设置
- 下次打开时恢复上次的状态

### 3. 快捷键支持
- 添加快捷键切换收缩状态（如 Ctrl+Shift+C）
- 添加快捷键切换联网搜索（如 Ctrl+Shift+W）

### 4. 高级联网搜索选项
- 搜索引擎选择
- 搜索结果数量设置
- 搜索时间范围设置
- 搜索语言设置

### 5. 视觉优化
- 添加更多动画效果
- 优化移动端显示
- 添加工具提示（Tooltip）
- 改进无障碍访问（ARIA 标签）

## 使用示例

### 收缩输入框
```typescript
// 点击收缩按钮
<button onClick={() => setIsCollapsed(!isCollapsed)}>
  {isCollapsed ? '展开' : '收缩'}
</button>
```

### 启用联网搜索
```typescript
// 勾选联网搜索
<input
  type="checkbox"
  checked={enableWebSearch}
  onChange={(e) => setEnableWebSearch(e.target.checked)}
/>
```

### 发送消息时使用联网搜索
```typescript
// 在发送逻辑中使用
const handleSend = () => {
  if (enableWebSearch) {
    // 执行联网搜索逻辑
    onSend(query, files, { webSearch: true })
  } else {
    // 正常发送
    onSend(query, files)
  }
}
```

## 兼容性

- ✅ 支持所有现代浏览器
- ✅ 支持移动端
- ✅ 支持暗色模式
- ✅ 支持键盘导航
- ✅ 支持屏幕阅读器

## 测试建议

1. **功能测试**:
   - 测试收缩/展开功能
   - 测试联网搜索开关
   - 测试状态切换的流畅性

2. **视觉测试**:
   - 检查动画效果
   - 检查不同屏幕尺寸下的显示
   - 检查暗色模式下的显示

3. **交互测试**:
   - 测试快速点击
   - 测试键盘操作
   - 测试触摸操作（移动端）

4. **集成测试**:
   - 测试与现有功能的兼容性
   - 测试文件上传功能
   - 测试语音输入功能

## 相关文件

- `web/app/components/base/chat/chat/chat-input-area/index.tsx` - 主组件（已修改）
- `web/app/components/base/chat/chat/chat-input-area/hooks.ts` - 钩子函数
- `web/app/components/base/chat/chat/chat-input-area/operation.tsx` - 操作按钮组件

## 注意事项

1. **联网搜索功能需要后端支持** - 目前只是前端开关，需要后端实现实际的联网搜索逻辑
2. **状态不持久化** - 刷新页面后状态会重置，建议添加持久化
3. **移动端优化** - 在小屏幕上可能需要调整布局
4. **性能考虑** - 动画效果在低端设备上可能需要优化
