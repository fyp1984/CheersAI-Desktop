# Dify 项目启动状态

**更新时间**: 2026-05-11 15:25

## ✅ 所有服务已成功启动

### 本地服务 (Terminal)

| 服务 | 端口 | Terminal ID | 状态 | 说明 |
|------|------|-------------|------|------|
| 前端 (Next.js) | 3000 | 9 | ✅ 运行中 | http://localhost:3000 |
| API (Waitress) | 9000 | 7 | ✅ 运行中 | http://127.0.0.1:9000 |
| Celery Worker | - | 3 | ✅ 运行中 | 后台任务处理 |
| Celery Beat | - | 4 | ✅ 运行中 | 定时任务调度 |

### Docker 服务

| 服务 | 容器名 | 端口映射 | 状态 | 说明 |
|------|--------|----------|------|------|
| PostgreSQL | dify-postgres | 5432:5432 | ✅ 运行中 (healthy) | 数据库 |
| Redis | dify-redis | 6700:6379 | ✅ 运行中 (healthy) | 缓存和消息队列 |
| Weaviate | dify-weaviate | 8081:8080 | ✅ 运行中 | 向量数据库 |
| Plugin Daemon | dify-plugin-daemon | 5012-5013:5012-5013 | ✅ 运行中 | 插件守护进程 |

## 配置说明

### API 端口变更
- **原端口**: 5001 (Windows 保留端口，无法使用)
- **新端口**: 9000
- **原因**: Windows 系统保留了 4907-5006 端口范围，导致 5001 端口无法绑定

### 前端配置 (web/.env)
```env
NEXT_PUBLIC_API_PREFIX=http://localhost:9000/console/api
NEXT_PUBLIC_PUBLIC_API_PREFIX=http://localhost:9000/api
```

### 数据库迁移
- ✅ 已执行 `flask db upgrade`
- ✅ 所有数据表已创建

## 访问地址

- **前端界面**: http://localhost:3000
- **API 服务**: http://127.0.0.1:9000
- **API 文档**: http://127.0.0.1:9000/console/api/docs

## FileBay 集成状态

### ✅ FileBay 内置工具已集成

**实现方式**: 作为 Dify 内置工具直接集成，无需安装插件

**工具位置**: `api/core/tools/builtin_tool/providers/filebay/`

**可用工具**:
1. **read_file** - 从 FileBay 仓库读取文件内容
2. **write_file** - 向 FileBay 仓库写入或更新文件 ⭐ 新增
3. **list_files** - 列出 FileBay 仓库中的文件和目录

**使用方式**:
1. 在 Dify 控制台的 **工具** 页面配置 FileBay 凭证
2. 在对话应用、工作流或探索应用中添加 FileBay 工具
3. 通过自然语言或工作流节点调用 FileBay 功能

**文件上传集成**:
- ✅ 支持将用户上传的文件保存到 FileBay
- ✅ 支持文件的创建和更新
- ✅ 支持自定义提交信息
- ✅ 可在工作流中实现自动化文件管理

**详细文档**: 
- `FileBay内置工具使用说明.md`
- `文件上传到FileBay集成方案.md` ⭐ 新增

## FileBay 插件状态（已废弃）

~~**名称**: filebay-sync~~
~~**版本**: 0.0.1~~
~~**包文件**: filebay_plugin.difypkg~~

**注意**: 插件方式已被内置工具取代，推荐使用内置工具方式。

## 重启服务命令

### 停止所有服务
```bash
# 停止本地服务 (在对应的 Terminal 中按 Ctrl+C)
# 或使用 Kiro 停止进程

# 停止 Docker 服务
docker compose -f docker-compose.dev.yaml down
```

### 启动所有服务
```bash
# 1. 启动 Docker 服务
docker compose -f docker-compose.dev.yaml up -d

# 2. 启动 API (在 api 目录)
uv run waitress-serve --host=127.0.0.1 --port=9000 --threads=4 app:app

# 3. 启动 Celery Worker (在 api 目录)
uv run celery -A app.celery worker -P threads -c 2 --loglevel INFO -Q dataset,priority_dataset,priority_pipeline,pipeline,mail,ops_trace,app_deletion,plugin,workflow_storage,conversation,workflow,schedule_poller,schedule_executor,triggered_workflow_dispatcher,trigger_refresh_executor,retention

# 4. 启动 Celery Beat (在 api 目录)
uv run celery -A app.celery beat --loglevel INFO

# 5. 启动前端 (在 web 目录)
pnpm dev
```

## 故障排查

### 前端无法连接 API
1. 检查 API 是否在 9000 端口运行
2. 检查 `web/.env` 中的 API 地址配置
3. 重启前端服务以加载新的环境变量

### 数据库连接错误
1. 检查 Docker 容器是否运行: `docker ps`
2. 检查 PostgreSQL 端口: 5432
3. 检查 `api/.env` 中的数据库连接配置

### 插件安装失败
1. 检查 Plugin Daemon 是否运行
2. 检查 `docker/middleware.env` 中 `FORCE_VERIFYING_SIGNATURE=false`
3. 查看插件日志获取详细错误信息

## 注意事项

1. **端口冲突**: 确保 3000, 9000, 5432, 6700, 8081, 5012-5013 端口未被占用
2. **环境变量**: 修改 `.env` 文件后需要重启对应服务
3. **数据库迁移**: 更新代码后记得运行 `flask db upgrade`
4. **插件签名**: 开发环境已禁用签名验证，生产环境需要启用


## 系统初始化状态

✅ **系统已完成初始化**
- 初始化时间: 2026-05-11 15:28:08
- 数据库版本: 1.12.0
- 管理员账户: 1@qq.com

## 访问系统

### 登录信息
- **登录页面**: http://localhost:3000/signin
- **管理员邮箱**: 1@qq.com
- **密码**: (你之前设置的密码)

### 如果忘记密码
可以通过以下命令重置密码：
```bash
cd api
uv run flask reset-password --email 1@qq.com --new-password your_new_password
```

## 下一步操作

1. **登录系统**: 
   - 访问 http://localhost:3000/signin
   - 使用管理员账户登录

2. **安装 FileBay 插件**:
   - 登录后进入 "工具" -> "插件" 页面
   - 点击 "安装插件" 按钮
   - 上传 `filebay_plugin.difypkg` 文件
   - 等待安装完成

3. **使用插件**: 
   - 创建或编辑应用
   - 在工具配置中添加 FileBay Sync
   - 配置 FileBay 连接信息
   - 在工作流中使用读取/写入/列出文件功能

## 常见问题

### Q: 访问 http://localhost:3000 显示安装页面并报 403 错误
**A**: 这是正常的，因为系统已经初始化完成。请直接访问登录页面: http://localhost:3000/signin

### Q: 如何查看所有账户
```bash
docker exec dify-postgres psql -U postgres -d dify -c "SELECT email, name, status FROM accounts;"
```

### Q: 如何创建新的管理员账户
```bash
cd api
uv run flask create-account --email new@example.com --name "New Admin" --password your_password
```


## 已修复的问题

### ✅ Marketplace 插件加载失败 (400)
**问题**: 前端尝试从 `marketplace.dify.ai` 加载推荐插件时失败，导致 `TypeError: Failed to fetch` 错误
**原因**: Marketplace API 端点不可用或未配置
**解决方案**: 
1. 在 `web/.env` 中将 Marketplace 配置设置为空值
2. 在 `web/service/client.ts` 中添加了 marketplace 未配置时的早期返回逻辑
3. 重启前端服务 (Terminal 26)

**影响**: 不影响本地插件安装和使用，只是无法从官方市场浏览推荐插件

### ⚠️ 其他非关键错误（可忽略）
1. **GPT-5.1 模型参数错误**: 系统尝试加载不存在的模型，不影响功能
2. **Moonshot 图标加载失败**: 图标文件缺失，不影响功能使用
