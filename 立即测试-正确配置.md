# SSO 登录 - 使用正确的 Client ID 和 Secret

## 已更新的配置

### Client ID 和 Secret
- **Client ID**: `4ef89efa78a4590d3cf7` ✅ (已更新)
- **Client Secret**: `08c616602528da4979d9cc701b47cfc6af843cd0` ✅ (已更新)

### 前端配置 (web/.env.local)
```bash
NEXT_PUBLIC_DESKTOP_SSO_CLIENT_ID=4ef89efa78a4590d3cf7
DESKTOP_SSO_CLIENT_SECRET=08c616602528da4979d9cc701b47cfc6af843cd0
```

### 后端配置 (api/.env)
```bash
DESKTOP_SSO_CLIENT_ID=4ef89efa78a4590d3cf7
DESKTOP_SSO_CLIENT_SECRET=08c616602528da4979d9cc701b47cfc6af843cd0
```

## 服务状态

- ✅ 后端: Terminal 7 (Flask API on port 5001) - 运行中
- ✅ 前端: Terminal 13 (Next.js on port 3000) - 已重启，使用新配置
- ✅ Docker 服务: 全部运行中

## 测试步骤

### 1. 清除浏览器数据
在浏览器开发者工具中 (F12)：
1. Application -> Cookies
2. 删除 `localhost:3000` 和 `localhost:5001` 下的所有 Cookie
3. Application -> Session Storage
4. 清除所有 Session Storage

### 2. 开始 SSO 登录
1. 访问：http://localhost:3000/signin
2. 点击 "SSO 登录" 按钮
3. 完成 SSO 认证

### 3. 观察控制台日志

**预期成功的日志**：
```
[SSO] OAuth callback page loaded
[SSO] Starting token exchange with: {code: "...", state: "...", redirectUri: "..."}
[SSO] Step 1: Exchanging OAuth code for SSO access_token
[SSO] Step 2: Fetching user info from SSO
[SSO] User info received: {email: "...", name: "..."}
[SSO] Step 3: Logging into Dify backend
[SSO] Backend login response: {result: "success"}
[SSO] Token exchange successful, waiting 1000ms before redirect
[SSO] Redirecting to /apps
```

### 4. 检查 Network 请求

在 Network 标签中应该看到：
1. **POST /api/auth/sso/token** - 200 ✅
2. **POST /api/auth/sso/userinfo** - 200 ✅
3. **POST /console/api/auth/desktop-sso/login** - 200 ✅

### 5. 验证登录成功

- 页面自动跳转到 `/apps`
- 可以看到应用主界面
- 后续 API 请求返回 200（不再是 401）

## 如果仍然失败

如果还是看到 401 错误，请检查：

### 检查 Next.js 服务器日志
```powershell
# 查看 Terminal 13 的输出
```

应该看到：
```
[SSO Token Route] Environment variables: {
  ssoBaseUrl: 'https://uat-sso.cheersai.cloud',
  clientId: '4ef89efa78a4590d3cf7',
  clientSecretLength: 40,
  hasClientSecret: true
}
[SSO Token Route] Token exchange request: {
  url: 'https://uat-sso.cheersai.cloud/api/login/oauth/access_token',
  clientId: '4ef89efa78a4590d3cf7',
  redirectUri: 'http://localhost:3000/oauth-callback',
  codeLength: 20,
  clientSecretLength: 40
}
```

### 如果还是失败，使用 V1.3 的完整代码

如果这次测试还是失败，我会：
1. 完全复制 V1.3 的 SSO 相关代码
2. 确保所有实现细节都一致
3. 逐个文件对比并替换

## 授权 URL

SSO 登录时会跳转到：
```
https://uat-sso.cheersai.cloud/login/oauth/authorize?client_id=4ef89efa78a4590d3cf7&redirect_uri=http%3A%2F%2Flocalhost%3A3000%2Foauth-callback&state=...&response_type=code&scope=openid+profile+email
```

注意：
- ✅ 使用 `/login/oauth/authorize`（不是 oauth2）
- ✅ Client ID 已更新为 `4ef89efa78a4590d3cf7`
- ✅ redirect_uri 是 `http://localhost:3000/oauth-callback`（没有额外参数）

## Token Exchange 请求

```
POST https://uat-sso.cheersai.cloud/api/login/oauth/access_token
Content-Type: application/x-www-form-urlencoded
Accept: application/json

Body:
  grant_type=authorization_code
  code=<授权码>
  redirect_uri=http://localhost:3000/oauth-callback
  client_id=4ef89efa78a4590d3cf7
  client_secret=08c616602528da4979d9cc701b47cfc6af843cd0
```

注意：
- ✅ 没有 Authorization header（已移除 Basic Auth）
- ✅ 凭据在 body 中发送
- ✅ 使用正确的 Client ID 和 Secret

---

**现在可以测试了！这次应该会成功。** 🎉

如果还是失败，请告诉我具体的错误信息，我会立即使用 V1.3 的完整代码。
