# Token 配额状态卡片 - 更新说明

## 📋 更新内容

根据你的需求，我已经将配额显示方式修改为：
- ❌ 移除了独立的"配额管理"标签页
- ✅ 在 Token 计费页面顶部添加了配额状态卡片
- ✅ 显示剩余额度和时间信息

---

## 🎯 新的显示方式

### 位置
**设置 → Token 计费** 页面顶部

### 显示内容

```
┌─────────────────────────────────────────────────────┐
│ ✅ 配额充足  使用云端模型                    [刷新]  │
│                                                     │
│ 剩余 55,000 tokens                                  │
│ 每天配额 100,000 tokens · 18小时30分钟后重置        │
│                                                     │
│ 已使用 45.0%                                        │
│ ████████░░░░░░░░░░ 45,000 / 100,000                │
└─────────────────────────────────────────────────────┘
```

---

## ✨ 功能特性

### 1. 配额状态徽章
- ✅ **配额充足** - 绿色徽章
  - 显示"使用云端模型"
  - 进度条为绿色/黄色/红色（根据使用率）
  
- ⚠️ **配额已用完** - 黄色徽章
  - 显示"已切换到本地模型"
  - 进度条为红色

### 2. 剩余额度显示
- 大字体显示剩余 Token 数
- 例如：**剩余 55,000 tokens**

### 3. 配额信息
- 显示配额周期（每小时/每天/每周/每月）
- 显示配额上限
- 例如：**每天配额 100,000 tokens**

### 4. 重置时间倒计时 ⏰
- 实时显示距离配额重置还有多长时间
- 格式：
  - `X天后重置` - 超过 24 小时
  - `X小时Y分钟后重置` - 1-24 小时
  - `X分钟后重置` - 少于 1 小时
  - `即将重置` - 少于 1 分钟

### 5. 使用进度条
- 可视化显示配额使用情况
- 颜色编码：
  - 🟢 绿色（0-70%）：配额充足
  - 🟡 黄色（70-90%）：配额紧张
  - 🔴 红色（90-100%）：配额即将用完

### 6. 详细使用数据
- 显示已使用百分比
- 显示已使用 / 总配额
- 例如：**45,000 / 100,000**

### 7. 手动刷新按钮
- 右上角刷新按钮
- 点击立即更新配额信息
- 刷新时显示旋转动画

### 8. 自动刷新
- 每 60 秒自动刷新一次
- 保持数据实时更新

---

## 📱 响应式设计

### 桌面端
```
┌──────────────────────────────────────────────┐
│ ✅ 配额充足  使用云端模型           [刷新]    │
│                                              │
│ 剩余 55,000 tokens                           │
│ 每天配额 100,000 tokens · 18小时30分钟后重置 │
│                                              │
│ 已使用 45.0%        45,000 / 100,000        │
│ ████████░░░░░░░░░░                          │
└──────────────────────────────────────────────┘
```

### 移动端
```
┌────────────────────────┐
│ ✅ 配额充足      [刷新] │
│ 使用云端模型            │
│                        │
│ 剩余 55,000 tokens     │
│ 每天配额 100,000       │
│ 18小时30分钟后重置     │
│                        │
│ 已使用 45.0%           │
│ 45,000 / 100,000      │
│ ████████░░░░░░░░░░    │
└────────────────────────┘
```

---

## 🎨 UI 示例

### 配额充足状态
```
┌─────────────────────────────────────────────────────┐
│ ✅ 配额充足  使用云端模型                    [🔄]   │
│                                                     │
│ 剩余 85,000 tokens                                  │
│ 每天配额 100,000 tokens · 20小时15分钟后重置        │
│                                                     │
│ 已使用 15.0%                                        │
│ ███░░░░░░░░░░░░░░░░░ 15,000 / 100,000              │
└─────────────────────────────────────────────────────┘
```

### 配额紧张状态（70-90%）
```
┌─────────────────────────────────────────────────────┐
│ ✅ 配额充足  使用云端模型                    [🔄]   │
│                                                     │
│ 剩余 20,000 tokens                                  │
│ 每天配额 100,000 tokens · 5小时30分钟后重置         │
│                                                     │
│ 已使用 80.0%                                        │
│ ████████████████░░░░ 80,000 / 100,000              │
└─────────────────────────────────────────────────────┘
```

### 配额即将用完状态（90-100%）
```
┌─────────────────────────────────────────────────────┐
│ ✅ 配额充足  使用云端模型                    [🔄]   │
│                                                     │
│ 剩余 5,000 tokens                                   │
│ 每天配额 100,000 tokens · 2小时15分钟后重置         │
│                                                     │
│ 已使用 95.0%                                        │
│ ███████████████████░ 95,000 / 100,000              │
└─────────────────────────────────────────────────────┘
```

### 配额已用完状态
```
┌─────────────────────────────────────────────────────┐
│ ⚠️ 配额已用完  已切换到本地模型              [🔄]   │
│                                                     │
│ 剩余 0 tokens                                       │
│ 每天配额 100,000 tokens · 1小时30分钟后重置         │
│                                                     │
│ 已使用 100.0%                                       │
│ ████████████████████ 100,000 / 100,000             │
└─────────────────────────────────────────────────────┘
```

---

## 📁 文件结构

```
web/app/components/header/account-setting/token-billing-page/
├── index.tsx                    # 主页面（已更新）
├── quota-status-card.tsx        # 配额状态卡片（新增）
└── quota-management.tsx         # 配额管理组件（已弃用）
```

---

## 🔧 技术实现

### 组件：QuotaStatusCard

**位置**: `web/app/components/header/account-setting/token-billing-page/quota-status-card.tsx`

**功能**:
1. 调用 API 获取配额信息
2. 计算剩余时间
3. 显示配额状态
4. 自动刷新（每 60 秒）
5. 手动刷新按钮

**API 调用**:
```typescript
fetch('/console/api/token-quota/check', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ tokens_to_use: 0 })
})
```

**时间计算**:
```typescript
const getTimeRemaining = (endTime: string) => {
  const end = new Date(endTime).getTime()
  const now = new Date().getTime()
  const diff = end - now

  if (diff <= 0) return '即将重置'

  const hours = Math.floor(diff / (1000 * 60 * 60))
  const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60))

  if (hours > 24) {
    const days = Math.floor(hours / 24)
    return `${days}天后重置`
  }
  if (hours > 0)
    return `${hours}小时${minutes}分钟后重置`

  return `${minutes}分钟后重置`
}
```

---

## 🎯 使用方式

### 1. 访问页面
1. 打开浏览器访问 http://localhost:3000
2. 登录系统
3. 点击右上角头像
4. 选择"设置"
5. 点击"Token 计费"

### 2. 查看配额状态
- 页面顶部会显示配额状态卡片
- 自动显示剩余额度和重置时间
- 无需切换标签页

### 3. 刷新数据
- 点击右上角的刷新按钮
- 或等待自动刷新（每 60 秒）

---

## 📊 数据更新频率

- **自动刷新**: 每 60 秒
- **手动刷新**: 点击刷新按钮
- **页面加载**: 首次加载时立即获取

---

## 🎨 颜色方案

### 徽章颜色
- **绿色** (`bg-success-50 text-success-600`): 配额充足
- **黄色** (`bg-warning-50 text-warning-600`): 配额已用完

### 进度条颜色
- **绿色** (`bg-primary-600`): 0-70%
- **黄色** (`bg-warning-600`): 70-90%
- **红色** (`bg-error-600`): 90-100%

---

## ✅ 完成状态

- ✅ 创建配额状态卡片组件
- ✅ 集成到 Token 计费页面顶部
- ✅ 移除独立的配额管理标签页
- ✅ 显示剩余额度
- ✅ 显示重置时间倒计时
- ✅ 显示使用进度条
- ✅ 自动刷新机制
- ✅ 手动刷新按钮
- ✅ 响应式设计
- ✅ 前端编译成功

---

## 🎉 总结

现在 Token 配额信息会直接显示在 Token 计费页面的顶部，用户可以：

1. **一目了然** - 打开页面就能看到配额状态
2. **剩余额度** - 清楚显示还剩多少 tokens
3. **重置时间** - 知道什么时候配额会重置
4. **使用进度** - 可视化显示使用情况
5. **实时更新** - 自动刷新保持数据最新

**不需要切换标签页，所有信息都在一个页面上！** 🎊
