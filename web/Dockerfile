# base image
FROM node:24-alpine AS base
LABEL maintainer="takatost@gmail.com"

# if you located in China, you can use aliyun mirror to speed up
# RUN sed -i 's/dl-cdn.alpinelinux.org/mirrors.aliyun.com/g' /etc/apk/repositories

# if you located in China, you can use taobao registry to speed up
# RUN npm config set registry https://registry.npmmirror.com

RUN apk add --no-cache tzdata
RUN corepack enable
ENV PNPM_HOME="/pnpm"
ENV PATH="$PNPM_HOME:$PATH"
ARG NEXT_PUBLIC_BASE_PATH=""
ENV NEXT_PUBLIC_BASE_PATH="$NEXT_PUBLIC_BASE_PATH"


# install packages
FROM base AS packages

WORKDIR /app/web

COPY package.json pnpm-lock.yaml /app/web/
COPY .corepack/pnpm/10.27.0 /opt/pnpm/10.27.0

# Reuse the host-cached pnpm bundle so Docker builds don't depend on external network access.
RUN ln -sf /opt/pnpm/10.27.0/bin/pnpm.cjs /usr/local/bin/pnpm \
    && ln -sf /opt/pnpm/10.27.0/bin/pnpx.cjs /usr/local/bin/pnpx

RUN pnpm install --frozen-lockfile

# build resources
FROM packages AS builder
WORKDIR /app/web
COPY . .

ARG NEXT_BUILD_NODE_OPTIONS="--max-old-space-size=4096"
ENV NODE_OPTIONS="${NEXT_BUILD_NODE_OPTIONS}"
ENV CONSOLE_API_PREFIX=http://api:5001/console/api
ENV INTERNAL_API_BASE_URL=http://api:5001
RUN pnpm build:docker

FROM packages AS dev
WORKDIR /app/web
COPY . .
ENV NODE_ENV=development
ENV PORT=3000
CMD ["pnpm","dev","-H","0.0.0.0","-p","3000"]


# production stage
FROM base AS production

ENV NODE_ENV=production
ENV EDITION=SELF_HOSTED
ENV DEPLOY_ENV=PRODUCTION
ENV CONSOLE_API_URL=http://127.0.0.1:5001
ENV APP_API_URL=http://127.0.0.1:5001
ENV INTERNAL_CONSOLE_API_PREFIX=http://127.0.0.1:5001/console/api
ENV INTERNAL_API_BASE_URL=http://127.0.0.1:5001
ENV MARKETPLACE_API_URL=https://marketplace.dify.ai
ENV MARKETPLACE_URL=https://marketplace.dify.ai
ENV PORT=3000
ENV NEXT_TELEMETRY_DISABLED=1
ENV PM2_INSTANCES=2

# set timezone
ENV TZ=UTC
RUN ln -s /usr/share/zoneinfo/${TZ} /etc/localtime \
    && echo ${TZ} > /etc/timezone

# global runtime packages
RUN npm install -g pm2


# Create non-root user
ARG dify_uid=1001
RUN addgroup -S -g ${dify_uid} dify && \
    adduser -S -u ${dify_uid} -G dify -s /bin/ash -h /home/dify dify && \
    mkdir /app && \
    mkdir /.pm2 && \
    chown -R dify:dify /app /.pm2


WORKDIR /app/web

COPY --from=builder --chown=dify:dify /app/web/public ./public
COPY --from=builder --chown=dify:dify /app/web/.next/standalone ./
COPY --from=builder --chown=dify:dify /app/web/.next/static/ ./.next/static/

COPY --chown=dify:dify --chmod=755 docker/entrypoint.sh ./entrypoint.sh

ARG COMMIT_SHA
ENV COMMIT_SHA=${COMMIT_SHA}

USER dify
EXPOSE 3000
ENTRYPOINT ["/bin/sh", "./entrypoint.sh"]
