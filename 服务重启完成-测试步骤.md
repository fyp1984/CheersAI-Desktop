# 服务重启完成 - 测试步骤

## ✅ 已完成的操作

### 1. 修复 Docker 服务
- 修改 Weaviate 端口从 8080 改为 8081（避免与 Gitea 冲突）
- 更新 `api/.env` 中的 `WEAVIATE_ENDPOINT=http://127.0.0.1:8081`
- 重启所有 Docker 容器

### 2. 重启应用服务
- 停止旧的前端和后端进程
- 重新启动后端 Flask API (port 5001)
- 重新启动前端 Next.js (port 3000)

### 3. 当前服务状态

#### Docker 容器（全部正常运行）
- ✅ dify-postgres (port 5432) - healthy
- ✅ dify-redis (port 6700) - healthy
- ✅ dify-weaviate (port 8081) - running
- ✅ dify-plugin-daemon (port 5002-5003) - running

#### 应用服务
- ✅ 后端 API: http://localhost:5001
- ✅ 前端 Web: http://localhost:3000

---

## 🧪 测试步骤

### 步骤 1: 清除浏览器 Cookie（重要！）

1. 打开浏览器开发者工具（F12）
2. 进入 Application 标签
3. 展开 Cookies
4. 删除 `http://localhost:3000` 下的所有 Cookie
5. 删除 `http://localhost:5001` 下的所有 Cookie（如果有）

### 步骤 2: 访问登录页面

访问：http://localhost:3000/signin

### 步骤 3: 点击 SSO 登录

点击页面上的 "SSO 登录" 按钮

### 步骤 4: 完成 SSO 认证

1. 会跳转到 SSO 登录页面：https://uat-sso.cheersai.cloud
2. 输入用户名和密码
3. 完成登录

### 步骤 5: 验证登录成功

登录成功后应该：
1. 自动跳转回 http://localhost:3000
2. 看到应用主界面（不再是登录页面）
3. 不再有 401 错误

---

## 🔍 如何检查是否成功

### 1. 检查 Cookie

在浏览器开发者工具中：
- Application -> Cookies -> `http://localhost:3000`
- 应该看到以下 Cookie：
  - `access_token`
  - `refresh_token`
  - `csrf_token`

### 2. 检查 Network 请求

在 Network 标签中：
- 所有 API 请求的 URL 应该以 `/api/proxy/` 开头
- 状态码应该是 200（不是 401）
- Request Headers 中应该包含 Cookie

### 3. 检查控制台日志

在 Console 标签中：
- 应该看到 SSO 登录相关的日志
- 不应该有 401 或 opaqueredirect 错误

---

## ❌ 如果仍然有问题

### 问题 1: 仍然看到 401 错误

**可能原因**：
- 没有清除旧的 Cookie
- 浏览器缓存问题

**解决方案**：
1. 完全关闭浏览器
2. 重新打开浏览器
3. 清除所有 localhost 的 Cookie
4. 重新测试

### 问题 2: 页面一直加载

**可能原因**：
- 后端服务没有正确响应
- 代理配置问题

**解决方案**：
1. 检查后端日志：查看 Terminal 14 的输出
2. 检查前端日志：查看 Terminal 15 的输出
3. 检查浏览器控制台的错误信息

### 问题 3: Plugin Daemon 错误

**可能原因**：
- Plugin Daemon 容器没有正确启动

**解决方案**：
```bash
docker ps  # 检查容器状态
docker logs dify-plugin-daemon  # 查看日志
```

---

## 📝 关键配置文件

### 前端配置 (`web/.env.local`)
```bash
NEXT_PUBLIC_API_PREFIX=/api/proxy/console/api
NEXT_PUBLIC_PUBLIC_API_PREFIX=/api/proxy/api
API_URL=http://localhost:5001

NEXT_PUBLIC_DESKTOP_SSO_LOGIN_URL=https://uat-sso.cheersai.cloud
NEXT_PUBLIC_DESKTOP_SSO_CLIENT_ID=c98f7150fe9c044bf217
NEXT_PUBLIC_DESKTOP_SSO_PROTOCOL=oauth2
NEXT_PUBLIC_DESKTOP_SSO_ENABLED=true
```

### 后端配置 (`api/.env`)
```bash
COOKIE_DOMAIN=
PLUGIN_DAEMON_URL=http://127.0.0.1:5002
WEAVIATE_ENDPOINT=http://127.0.0.1:8081
```

### 代理配置 (`web/app/api/proxy/[...path]/route.ts`)
- 所有 `/api/proxy/*` 请求会被转发到 `http://localhost:5001/*`
- 这样前端和后端在浏览器看来是同一个域名
- Cookie 可以正常工作

---

## 🎯 预期结果

如果一切正常，你应该能够：
1. ✅ 成功完成 SSO 登录
2. ✅ 看到应用主界面
3. ✅ 所有 API 请求返回 200
4. ✅ 没有 401 或跨域错误
5. ✅ Cookie 正确设置在 localhost:3000

---

## 📞 需要帮助？

如果测试过程中遇到任何问题，请提供：
1. 浏览器控制台的错误信息（Console 标签）
2. Network 标签中失败请求的详细信息
3. 后端日志（Terminal 14）
4. 前端日志（Terminal 15）

这样我可以更准确地诊断问题。
