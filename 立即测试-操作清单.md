# 立即测试 SSO 登录 - 操作清单

## ✅ 已完成的修复

1. ✅ 创建了 Next.js API routes (`/api/auth/sso/token` 和 `/api/auth/sso/userinfo`)
2. ✅ 更新了前端 SSO service 使用 V1.3 的实现
3. ✅ 修复了 OAuth callback 页面
4. ✅ 修改了 protocol 从 `oauth2` 改为 `oauth`
5. ✅ 添加了 `scope` 参数
6. ✅ 重启了前端服务

## 当前服务状态

✅ 后端: http://localhost:5001 (Terminal 7)
✅ 前端: http://localhost:3000 (Terminal 8)
✅ Docker 服务全部运行中

## SSO 登录流程（V1.3 版本）

```
1. 用户点击 SSO 登录
   ↓
2. 跳转到 SSO 服务器
   URL: https://uat-sso.cheersai.cloud/login/oauth/authorize
   参数:
   - client_id=c98f7150fe9c044bf217
   - redirect_uri=http://localhost:3000/oauth-callback
   - state=随机字符串
   - response_type=code
   - scope=openid profile email
   ↓
3. 用户在 SSO 页面登录
   ↓
4. SSO 重定向回 /oauth-callback?code=xxx&state=xxx
   ↓
5. 前端调用 /api/auth/sso/token (Next.js API route)
   - Next.js 调用 SSO API 交换 token
   - 获取 access_token
   - 保存到 Next.js Cookie (sso_access_token)
   ↓
6. 前端调用 /api/auth/sso/userinfo (Next.js API route)
   - Next.js 使用 access_token 获取用户信息
   - 返回 email, name
   ↓
7. 前端调用后端 /console/api/auth/desktop-sso/login
   - 传递 email, name
   - 后端创建/获取账户
   - 后端设置 Dify Cookie (access_token, refresh_token, csrf_token)
   ↓
8. 前端跳转到 /apps
   ↓
9. 登录成功！
```

## 现在测试！

### 步骤 1：打开浏览器

访问：http://localhost:3000/signin

### 步骤 2：点击 SSO 登录

应该跳转到：
```
https://uat-sso.cheersai.cloud/login/oauth/authorize?
  client_id=c98f7150fe9c044bf217&
  redirect_uri=http://localhost:3000/oauth-callback&
  state=随机字符串&
  response_type=code&
  scope=openid+profile+email
```

### 步骤 3：在 SSO 页面登录

输入用户名和密码

### 步骤 4：观察回调

登录成功后会跳转到：
```
http://localhost:3000/oauth-callback?code=xxx&state=xxx
```

### 步骤 5：查看控制台日志

#### 前端控制台（F12）

应该看到：
```
[SSO] OAuth callback page loaded
[SSO] Starting token exchange with: {code: "...", state: "...", redirectUri: "..."}
[SSO] Step 1: Exchanging OAuth code for SSO access_token
[SSO] Step 2: Fetching user info from SSO
[SSO] User info received: {email: "...", name: "..."}
[SSO] Step 3: Logging into Dify backend
[SSO] Calling backend /auth/desktop-sso/login with: {email: "...", name: "..."}
[SSO] Backend login response: {result: "success"}
[SSO] Token exchange successful, waiting 1000ms before redirect
[SSO] Redirecting to /apps
```

#### 后端日志

应该看到：
```
Desktop SSO login request received
Request data: {'email': '...', 'name': '...'}
Processing SSO login for: user@example.com
Found existing account for: user@example.com (或 Creating new account)
Generating tokens for: user@example.com
Setting cookies for: user@example.com
Desktop SSO Login success for: user@example.com
```

## 预期结果

✅ 跳转到 SSO 页面
✅ SSO 登录成功
✅ 跳转回 /oauth-callback
✅ 显示 "Completing SSO login..." 加载动画
✅ 前端控制台显示完整的 3 步流程
✅ 后端日志显示登录成功
✅ 自动跳转到 /apps 页面
✅ 页面显示用户信息
✅ 浏览器有 2 组 Cookie：
   - Next.js Cookie: sso_access_token, sso_refresh_token
   - Dify Cookie: access_token, refresh_token, csrf_token

## 如果失败

### 错误 1：Token exchange failed

检查：
- `web/.env.local` 中的 `DESKTOP_SSO_CLIENT_SECRET` 是否正确
- SSO API URL 是否正确
- 网络连接是否正常

### 错误 2：Failed to fetch user info

检查：
- SSO access_token 是否有效
- SSO userinfo endpoint 是否正确

### 错误 3：Backend login failed

检查：
- 后端服务是否运行
- 后端日志中的错误信息
- 数据库连接是否正常

## 重要提示

⚠️ 你需要真实的 `DESKTOP_SSO_CLIENT_SECRET`！

当前配置文件中的值是占位符 `your_client_secret_here`。

如果你有真实的 client_secret：
1. 更新 `web/.env.local` 中的 `DESKTOP_SSO_CLIENT_SECRET`
2. 重启前端服务（已经重启过了）
3. 测试 SSO 登录

## 与之前的区别

### 之前的方案（失败）
- 前端直接调用后端 `/console/api/auth/sso/token`
- 后端调用 SSO API（需要暴露 client_secret 到后端）
- 问题：跨域 Cookie 设置困难

### V1.3 方案（成功）
- 前端调用 Next.js API routes `/api/auth/sso/token`
- Next.js API route 调用 SSO API（client_secret 在 Next.js 环境变量中）
- Next.js 设置 SSO Cookie
- 前端再调用后端 `/auth/desktop-sso/login` 设置 Dify Cookie
- 优势：同源请求，Cookie 设置没有问题

## 现在可以测试了！

访问 http://localhost:3000/signin 开始测试 SSO 登录！
