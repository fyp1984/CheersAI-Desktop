# 项目启动完成 - V1.2 分支

## ✅ 已完成的操作

### 1. 代码同步和合并
- 当前分支：V1.2
- 从 V1.3 分支恢复了 SSO 认证文件：`api/controllers/console/auth/desktop_sso.py`
- 更新了配置文件以匹配本地开发环境

### 2. 配置更新

#### 后端配置 (`api/.env`)
```bash
# CORS 配置
CONSOLE_CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,*
WEB_API_CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,*

# Weaviate 端口（避免与 Gitea 冲突）
WEAVIATE_ENDPOINT=http://127.0.0.1:8081

# Plugin Daemon
PLUGIN_DAEMON_URL=http://127.0.0.1:5002

# SSO API
SSO_API_URL=https://uat-sso.cheersai.cloud/api
```

#### 前端配置 (`web/.env.local`)
```bash
# 直接连接后端（不使用代理）
NEXT_PUBLIC_API_PREFIX=http://localhost:5001/console/api
NEXT_PUBLIC_PUBLIC_API_PREFIX=http://localhost:5001/api

# SSO 配置
NEXT_PUBLIC_DESKTOP_SSO_LOGIN_URL=https://uat-sso.cheersai.cloud
NEXT_PUBLIC_DESKTOP_SSO_CLIENT_ID=c98f7150fe9c044bf217
NEXT_PUBLIC_DESKTOP_SSO_PROTOCOL=oauth2
NEXT_PUBLIC_DESKTOP_SSO_ENABLED=true
```

### 3. 服务状态

#### Docker 容器（全部运行中）
- ✅ dify-postgres (port 5432) - healthy
- ✅ dify-redis (port 6700) - healthy
- ✅ dify-weaviate (port 8081) - running
- ✅ dify-plugin-daemon (port 5002-5003) - running
- ✅ gitea (port 8080) - running
- ✅ casdoor SSO (port 18000) - running

#### 应用服务
- ✅ 后端 Flask API: http://localhost:5001 (Terminal 3)
- ✅ 前端 Next.js: http://localhost:3000 (Terminal 4)

---

## 🎯 主要变更说明

### 从云端同步的变更
根据 Git 历史，最近的变更包括：
1. **依赖更新**：lint-staged, sortablejs, googleapis-common-protos 等
2. **代码优化**：pre-commit lint 和 type 问题修复
3. **功能更新**：本地 stack 和 provider link 功能
4. **文件清理**：清理了一些过时的文件

### 本地恢复的文件
- `api/controllers/console/auth/desktop_sso.py` - SSO 认证接口（从 V1.3 分支恢复）

---

## 🧪 测试 SSO 登录

### 步骤 1: 清除浏览器 Cookie
1. 打开开发者工具（F12）
2. Application -> Cookies
3. 删除所有 localhost 的 Cookie

### 步骤 2: 访问登录页面
访问：http://localhost:3000/signin

**注意**：未登录时看到 401 错误是正常的！

### 步骤 3: SSO 登录
1. 点击 "SSO 登录" 按钮
2. 完成 SSO 认证
3. 应该成功登录并跳转到应用主界面

---

## 📝 关键文件位置

### 后端
- SSO 认证接口：`api/controllers/console/auth/desktop_sso.py`
- 环境配置：`api/.env`
- Docker 配置：`docker-compose.dev.yaml`

### 前端
- 环境配置：`web/.env.local`
- SSO 回调页面：`web/app/oauth-callback/page.tsx`
- SSO 认证组件：`web/app/signin/components/sso-auth.tsx`

---

## 🔧 常用命令

### 启动服务
```bash
# 启动 Docker 服务
docker-compose -f docker-compose.dev.yaml up -d

# 启动后端（在 api 目录）
.venv\Scripts\python.exe -m flask run --host 0.0.0.0 --port=5001 --debug

# 启动前端（在 web 目录）
pnpm dev
```

### 停止服务
```bash
# 停止 Docker 服务
docker-compose -f docker-compose.dev.yaml down

# 停止后端和前端：在对应的 Terminal 按 Ctrl+C
```

### 查看日志
```bash
# Docker 日志
docker logs dify-postgres
docker logs dify-r